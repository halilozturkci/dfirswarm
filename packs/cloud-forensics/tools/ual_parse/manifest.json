{
  "name": "ual_parse",
  "description": "Read a Microsoft 365 unified audit log export and explode the AuditData column, which is JSON and is where almost everything lives. Normalises each row, summarises by operation, user and address, and flags the operations that matter: inbox rules, forwarding, consents, anonymous links and mailbox reads.",
  "params": {
    "path": {
      "type": "string",
      "description": "A CSV or JSON export, or a directory of them"
    },
    "operations": {
      "type": "array",
      "description": "Keep only these operations"
    },
    "user": {
      "type": "string",
      "description": "Case-insensitive regex over the user"
    },
    "since": {
      "type": "string",
      "description": "ISO 8601 UTC lower bound"
    },
    "until": {
      "type": "string",
      "description": "ISO 8601 UTC upper bound"
    },
    "notable_only": {
      "type": "boolean",
      "description": "Only the operations this tool flags as worth a look"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum records returned inline when out_file is set (default 500); the complete match set is written to out_file"
    },
    "out_file": {
      "type": "string",
      "description": "Optional JSONL path for every matching normalised record; when absent every match is returned inline"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 300,
  "example": "{\"path\": \"inputs/ual.csv\", \"notable_only\": true, \"out_file\": \"work/ual.jsonl\"}",
  "by": "maintainer",
  "at": "2026-09-22T22:00:00.000Z",
  "version": 1,
  "sha256": "8f90c1723fce49b14c7ade9b4793b7c44afd2169bbd1c6550573e00bb646b6d0"
}
