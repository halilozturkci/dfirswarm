{
  "name": "fsevents_parse",
  "description": "Read /.fseventsd record files: the per-volume log of file creations, deletions, renames and modifications that macOS writes whether anyone wants it or not. Returns each path with its event id and decoded flags. Order, not time.",
  "params": {
    "path": {
      "type": "string",
      "description": "An /.fseventsd directory, or one record file"
    },
    "contains": {
      "type": "string",
      "description": "Case-insensitive regex over the path"
    },
    "flags": {
      "type": "array",
      "description": "Keep only records carrying these flags, e.g. [\"Removed\",\"Renamed\"]"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum records returned inline when out_file is set (default 2000); every matching record is written to out_file"
    },
    "out_file": {
      "type": "string",
      "description": "Optional JSONL path for every matching record; when absent every match is returned inline"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 300,
  "example": "{\"path\": \"work/extracted/.fseventsd\", \"flags\": [\"Removed\"], \"out_file\": \"work/fsevents.jsonl\"}",
  "by": "maintainer",
  "at": "2026-09-22T16:00:00.000Z",
  "version": 1,
  "sha256": "27e86450d5b8e9526caa3caa1f3dfb9857ae079df11a59f32d834a526abcff71"
}
