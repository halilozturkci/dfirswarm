{
  "name": "knowledgec_query",
  "description": "Read knowledgeC.db: which application was in the foreground and for how long, when the screen was on, when the device was locked, and what was browsed. Joins ZOBJECT to its metadata and converts every time from the Apple epoch.",
  "params": {
    "db": {
      "type": "string",
      "description": "A knowledgeC.db, copied out with any -wal beside it"
    },
    "stream": {
      "type": "string",
      "description": "A stream name or a LIKE pattern, e.g. '/app/inFocus' or '/app/%'"
    },
    "since": {
      "type": "string",
      "description": "Only entries starting at or after this ISO 8601 UTC time"
    },
    "until": {
      "type": "string",
      "description": "Only entries starting at or before this ISO 8601 UTC time"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum rows returned inline when out_file is set (default 500); every matching row is written to out_file"
    },
    "out_file": {
      "type": "string",
      "description": "Optional JSONL path for every matching row; when absent every row is returned inline"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 120,
  "example": "{\"db\": \"work/extracted/knowledgeC.db\", \"stream\": \"/app/inFocus\", \"out_file\": \"work/knowledgec.jsonl\"}",
  "by": "maintainer",
  "at": "2026-09-22T16:00:00.000Z",
  "version": 1,
  "sha256": "c1f8cb7f983036a53f953935cefda74475939648e6f9b6620ce3f779b6675049"
}
