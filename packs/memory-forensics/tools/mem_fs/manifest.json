{
  "name": "mem_fs",
  "description": "Mount a memory image with MemProcFS and return what it exposes: the process list with parents and paths, and the file tree the ordinary tools can then read. Says plainly when MemProcFS is not on the host rather than implying the memory was examined.",
  "params": {
    "path": {
      "type": "string",
      "description": "The memory image"
    },
    "mount": {
      "type": "string",
      "description": "Where to mount it: a directory under your own work/<your id>/ (the rest of work/ is read-only in a VM)"
    },
    "list": {
      "type": "string",
      "description": "A file to read in full or directory to list in full inside the mount (default: sys/proc/proc.txt, the process list)"
    },
    "timeout_seconds": {
      "type": "integer",
      "description": "Give up after this long (default 300)"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "requires": [
    "memprocfs"
  ],
  "timeout_seconds": 600,
  "example": "{\"path\": \"inputs/memory.raw\", \"mount\": \"work/<your id>/mem\"}",
  "by": "maintainer",
  "at": "2026-09-22T17:00:00.000Z",
  "version": 1,
  "sha256": "76a835988721aae84a4ddf35f066f2cea03ae361c33d55ec8bc1ccf8191986f0"
}
