{
  "name": "pcap_summary",
  "description": "Read a pcap or pcapng with no external dependency and return the shape of it: link type, snap length, time range, conversations with bytes and packets each way, grouped per session or per endpoint, top talkers, ports and protocols, and the connection start times a beacon check needs.",
  "params": {
    "path": {
      "type": "string",
      "description": "A .pcap or .pcapng file"
    },
    "top": {
      "type": "integer",
      "description": "How many conversations to return inline. When this omits rows, out_dir is required and keeps the complete conversations.tsv. By default every conversation is returned."
    },
    "host": {
      "type": "string",
      "description": "Only conversations involving this address"
    },
    "port": {
      "type": "integer",
      "description": "Only conversations involving this port"
    },
    "out_dir": {
      "type": "string",
      "description": "Optional empty directory under work/ for a complete conversations.tsv"
    },
    "with_starts": {
      "type": "boolean",
      "description": "Include each conversation's connection start times, for beacon_score"
    },
    "group": {
      "type": "string",
      "description": "session (default, one row per TCP session) or endpoint (one row per source, destination and service port, which is what a beacon check needs)"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 600,
  "example": "{\"path\": \"inputs/capture.pcap\", \"top\": 20, \"out_dir\": \"work/pcap-summary\", \"with_starts\": true}",
  "by": "maintainer",
  "at": "2026-09-22T19:00:00.000Z",
  "version": 1,
  "sha256": "6c91c7a4b91095ed4049aaf551a0b771a6a0b4906b198c6264d49295c6d43e4d"
}
