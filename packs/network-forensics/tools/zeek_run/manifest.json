{
  "name": "zeek_run",
  "description": "Run Zeek over a capture and return its structured logs: connections, DNS, HTTP, TLS, files and notices, as records rather than as a directory of TSV. The form every later question about a capture wants.",
  "params": {
    "path": {
      "type": "string",
      "description": "A .pcap or .pcapng file"
    },
    "out_dir": {
      "type": "string",
      "description": "Where Zeek's logs land, under work/"
    },
    "logs": {
      "type": "array",
      "description": "Which logs to return, e.g. [\"conn\",\"dns\",\"ssl\"] (default: all that were written)"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum records per log (default 200)"
    },
    "timeout_seconds": {
      "type": "integer",
      "description": "Give up after this long (default 900)"
    }
  },
  "requires": ["zeek"],
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 1200,
  "example": "{\"path\": \"inputs/capture.pcap\", \"out_dir\": \"work/zeek\", \"logs\": [\"conn\", \"dns\", \"ssl\"]}",
  "by": "maintainer",
  "at": "2026-09-22T19:00:00.000Z",
  "version": 1,
  "sha256": "82e7d20790ea209be3edd0554545ac7b040b09893bbdf730bdaada840bdfc577"
}
