{
  "name": "collection_index",
  "description": "Build the index the rest of the work needs: every file in a collection with its size, its sha256, the path it has now and the path it had on the machine, undoing the rewrites collectors make — a drive letter turned into a directory, a named stream's colon replaced, a reserved name escaped.",
  "params": {
    "root": {
      "type": "string",
      "description": "The collection directory"
    },
    "contains": {
      "type": "string",
      "description": "Case-insensitive regex over either path"
    },
    "hash": {
      "type": "boolean",
      "description": "Hash every file (default true; false is much faster on a large tree)"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum entries to return inline when out_file is set (default 2000); the complete index is written to out_file"
    },
    "out_file": {
      "type": "string",
      "description": "Optional JSONL path for the complete index; when absent every entry is returned inline"
    }
  },
  "runtime": "python3",
  "entry": "run.py",
  "timeout_seconds": 900,
  "example": "{\"root\": \"inputs/collection\", \"contains\": \"NTUSER\", \"out_file\": \"work/collection-index.jsonl\"}",
  "by": "maintainer",
  "at": "2026-09-22T23:30:00.000Z",
  "version": 1,
  "sha256": "6fe89e377aa3149734da6abf219ea13709224d9d0a414193c2e5b874ed20552f"
}
